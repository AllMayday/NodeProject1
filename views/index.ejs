<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ay this is working</title>
</head>
<body>
    <h1>Huh, definitely working</h1>
    <h2>Who knew?</h2>
    <hr>
    Name: <%= name %><br>
    Lang: <%= lang %><br>
    Company: <%= company %>

    <% items.forEach(function(item){ %>
    <p><%= item.name %> | <%= item.price %></p>    
    <% }) %>

    <% if(ShowMsg){ %>
    THIS IS AN ERROR MESSAGE
    <% }  else{ %> <!--ELSE NEEDS TO BE HERE, NOT BELOW-->
        NO ERRORS HERE BRO
    <% } %>
</body>
</html>